# Azure Speech 설정 (STT/TTS)
AZURE_SPEECH_API_KEY=your-api-key
AZURE_SPEECH_REGION=eastus
AZURE_SPEECH_ENDPOINT=https://your-region.stt.speech.microsoft.com

# ==========================================
# SuperTone API 설정
# ==========================================
SUPERTON_API_KEY=your-api-key
SUPERTON_VOICE_ID=your-voice-id

# ==========================================
# Wake Word & Sleep Mode 설정
# ==========================================
# 트리거 단어 사용 여부 (true/false)
# - true: 트리거 단어를 말해야 Wake mode로 전환 (기본값)
# - false: 트리거 단어 없이 바로 모든 말에 응답 (시끄러운 환경에 추천)
USE_TRIGGER_WORD=true

# 트리거 단어 (USE_TRIGGER_WORD가 true일 때만 사용)
TRIGGER_WORDS=치피,시피

# Sleep mode 타임아웃 (초) - 일정 시간 말이 없으면 Sleep mode로 전환
SLEEP_TIMEOUT=10.0

# ==========================================
# VAD (Voice Activity Detection) 설정
# 시끄러운 환경에서 음성 인식 개선을 위한 설정
# ==========================================
# 에너지 임계값: 소리 감지 민감도
# - 높을수록: 큰 소리만 감지 (배경 소음 무시) ✅
# - 낮을수록: 작은 소리도 감지 (매우 민감) ⚠️
# 조용한 환경: 0.005 (기본값, 매우 민감)
# 시끄러운 환경 (TV, 음악): 0.02 (4배 증가, 추천)
# 매우 시끄러운 환경 (거리, 공사): 0.05 (10배 증가)
VAD_ENERGY_THRESHOLD=0.005

# 침묵 시간: 말이 끝날 때까지 기다리는 시간 (초)
# - 높을수록: 말 끝까지 더 기다림 (정확한 인식) ✅
# - 낮을수록: 빠르게 응답 (반응 속도 향상) ⚠️
# 조용한 환경: 0.8 (기본값)
# 시끄러운 환경: 1.2 (추천)
# 매우 시끄러운 환경: 1.5
VAD_SILENCE_DURATION=0.8

# 최소 음성 시간: 음성으로 인식할 최소 길이 (초)
# - 높을수록: 긴 음성만 인식 (노이즈 무시) ✅
# - 낮을수록: 짧은 음성도 인식 (빠른 명령) ⚠️
# 조용한 환경: 0.3 (기본값)
# 시끄러운 환경: 0.5 (추천)
# 매우 시끄러운 환경: 0.8
VAD_MIN_SPEECH_DURATION=0.3

# 최대 녹음 시간: 무한정 녹음 방지, 최대 녹음 시간 (초)
# - 시끄러운 환경에서 음성 종료 감지가 안 될 때 강제 종료
# - 기본값: 10.0초 (대부분의 음성 명령에 충분)
# - 너무 짧으면: 긴 명령이 끊길 수 있음
# - 너무 길면: 배경 소음으로 인해 무한정 녹음될 수 있음
VAD_MAX_RECORDING_DURATION=10.0

# 에너지 감소 비율: 상대적 에너지 감소 감지 민감도 (0.0 ~ 1.0)
# - 시끄러운 환경에서 배경 소음 때문에 음성 종료를 감지하기 어려울 때 사용
# - 음성 중 최고 에너지의 이 비율 이하로 떨어지면 종료로 판단
# - 0.5 = 최고 에너지의 50% 이하로 떨어지면 종료 (기본값)
# - 낮을수록 (0.3~0.4): 더 빨리 종료 (민감) ✅ 시끄러운 환경 추천
# - 높을수록 (0.6~0.7): 더 오래 기다림 (둔감) ⚠️ 조용한 환경
# 조용한 환경: 0.5 (기본값)
# 시끄러운 환경: 0.3~0.4 (추천 - 더 빠른 종료)
# 매우 시끄러운 환경: 0.3 (강력 추천)
VAD_ENERGY_DROP_RATIO=0.5

# ==========================================
# Azure OpenAI 설정
# ==========================================
AZURE_OPENAI_ENDPOINT=https://your-endpoint.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o

# ==========================================
# Azure PostgreSQL 설정
# ==========================================
DB_HOST=your-db-host.postgres.database.azure.com
DB_PORT=5432
DB_NAME=your-db-name
DB_USER=your-db-user
DB_PASSWORD=your-db-password

# ==========================================
# 디바이스 시리얼 (앱에서 사용)
# ==========================================
DEVICE_SERIAL=your-device-serial

# ==========================================
# 사용자 이메일 (로그인한 사용자)
# ==========================================
USER_EMAIL=your-email@example.com

# ==========================================
# 시스템 프롬프트
# ==========================================
SYSTEM_PROMPT_JARVIS_4=your-prompt
SYSTEM_PROMPT_JARVIS_35=your-prompt
SYSTEM_PROMPT_TERMINAL=your-prompt
SYSTEM_PROMPT_CHIPI=your-prompt